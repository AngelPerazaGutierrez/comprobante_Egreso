// var formulario = document.getElementById("comprobante_egreso");
// formulario.addEventListener("submit", function(event){
//   event.preventDefault();
  
//   var formData = {}  
//   var elementosFormulario = formulario.elements;  
  
//   for (var i = 0; i < elementosFormulario.length; i++) {
//     var elemento = elementosFormulario[i];
//     if(elemento.type !== "submit" && elemento.name) {
//       formData[elemento.name] = elemento.value;      
//     }
//   }

//   var datos = JSON.stringify(formData)
//   console.log(datos)